"""Agentic auth-security testing harness.

`agent_core` is the domain-agnostic machinery (loop, tools, report). `modules/<name>`
contribute a prompt, a checklist, and domain tools. `auth` is the first module.
"""
